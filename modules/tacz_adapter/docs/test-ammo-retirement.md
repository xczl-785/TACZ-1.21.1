# 两种开发测试弹退役

2026-09-13，所有者明确要求：真实弹药体系已接入，删除两种无继续使用价值的开发物品。已删除`tarkov_content:test_flesh_9x19`、`tarkov_content:test_ap_9x19`的注册类及8份专属模型/语言/身份/配置/类别资源；逐文件路径、旧哈希与基线见[对账JSON](test-ammo-retirement.json)。不移动到examples，不保留别名或重复注册。

开发命令`/tacztest kit`、`select_flesh`、`select_ap`和`/armortest shoot_me flesh|ap`改用fork已有9×19 Luger CCI（70/10/38）与PBP gzh（44/39/55），依次为伤害/穿透/甲伤。命令名保留，物品使用正式名称与数据，不创建新测试弹。86种真实弹、15枪、配件、射击与抛壳资源、运行锁/Jar不变。

回归继续验证所有枪与真实弹种的供弹、保存、发射和命中；开发命令也实际发射真实弹种。仅隔离护甲数值回归保留30/40/50纯参数夹具，在发射后设置测试投射物快照；它不是Item，不注册、不能领取、不能装填，且只允许smoke环境调用。这样保留已有护甲公式的独立预期值，不伪造真实弹药数据。库存接纳回归直接覆盖86真实弹；退役ID在注册表、目录和领取请求中均拒绝。

旧开发存档不批量改写、不映射为其他弹、不自动返还。既有测试弹物品会因注册退役失效；历史存档/备份可用删除前版本查看。日常当前玩家记录只读核对，未修改任何世界；不宣称扫描过所有旧世界、区块或备份。正式Jar此前也不含这两种测试弹。

验证通过：`:tacz_adapter:runAdapterSmoke :tacz_adapter:test`（20秒），随后`:tacz_adapter:runClientSmoke build -x :tacz_adapter:runAdapterSmoke`（29秒，复用刚导出的服务器目录与缓存）。覆盖189项目录领取/保存、退役ID拒绝、15枪匹配真实弹种、实际命中/护甲/开发命令、客户端目录与完整构建；正式Jar缺席检查通过。日志哈希与PASS标记存入对账JSON。所有者实机接受仍待亲自完成。
