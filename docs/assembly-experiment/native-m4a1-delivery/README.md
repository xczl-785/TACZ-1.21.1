# 原生 M4 模型与显示交付

后续公共资产批次已完成制作与打包：6枪口＋18弹匣/弹仓管变体，覆盖剩余15定义；尚未激活到其他组装枪。当前制品 `1.1.8-hotfix-r6-newmod.41571235`，已验收M4行为与资产保持。见 [公共库交付](../native-attachments-delivery/README.md)。

最新接入：临时预设编辑已完成，包含RK-1方向修正。源码 `21820224`，NewMod `673dba3`，制品 `1.1.8-hotfix-r6-newmod.21820224`。111项Java与正式/开发启动准备通过；用户已确认“全部验证通过”，本轮实机验收完成。工作台右上“编辑预设”进入，无背包消耗，关闭丢弃；见 [功能合同](临时预设编辑.md) 与 [证据](evidence/preset-delivery.json)。

最新修正：RK-1斜握把按用户装枪反馈改为相反的左右侧倾，持枪/组装台/图标同步；源码 `220e8d7e`，NewMod `0f37374`，制品 `1.1.8-hotfix-r6-newmod.220e8d7e`。工程检查通过，已随最新集成版本获用户验收，见 [证据](evidence/rk1-direction.json)。

当前批次（2026-09-16）：M4剩余28件非瞄具已制作，累计52件2447方块。源码 `01c69961`，NewMod `894a404`，制品 `1.1.8-hotfix-r6-newmod.01c69961`。101 Java测试、11编辑/批量模型测试及28件Blockbench真实往返通过；NewMod接入与实机状态见 [本批证据](evidence/remaining-delivery.json)。不新增其他枪械，15光学候选继续延后。详见 [说明与后续路线](非瞄具批量与框架加固.md)。

上一批战术护木/8替换枪托（源码 `b6399f96`、NewMod `400a159`）已获用户实机确认无误。历史工程证据见 [首批证据](evidence/batch-delivery.json)。

前次交付（2026-09-16）：修复第三人称只显示下机匣的物品入口，栏位改为当前装配图标并按可见边界缩放。源码 `2d583307`，NewMod 接入 `35a78c5`，制品 `1.1.8-hotfix-r6-newmod.2d583307`。90 Java 测试、构建与两套启动准备通过；实机待用户。见 [整枪显示说明与验收](整枪显示链路.md) 和 [本轮证据](evidence/display-delivery.json)。

前次交付（2026-09-16）：默认 15 件图标已统一枪口向左，并修正透明像素遮挡。源码 `71d0bcdd`，NewMod 接入 `94b1ce1`，制品 `1.1.8-hotfix-r6-newmod.71d0bcdd`。构建与正式/开发启动准备通过；未启动游戏，待用户核实。详见 [本轮证据](evidence/icon-delivery.json) 与 [新旧图标对照](evidence/icon-comparison.png)。用户核实后再考虑低成本批量制作，瞄具最后处理。

2026-09-16 用户授权：拆分默认完整 M4，带纹理替换游戏配件并接入 NewMod，由用户启动验收。

15 件独立方块模型已建立，583 方块；保留原生动画骨骼，工作台/图标/持枪高低模由同一编辑来源生成。修正 PNG 像素尺寸与 UV 尺寸混淆。CTR 仅替换本枪显示，其他枪与候选保留。来源和操作见[默认套件编辑入口](../../../modules/tacz_adapter/weapon-sources/native_m4a1/editable/README.md)。

15 件 Blockbench 加载/编译坐标与 UV 往返通过。已交付 `1.1.8-hotfix-r6-newmod.9da6cb13`，NewMod 接入 `e2da871`。83 项 Java 测试通过；正式/开发启动准备及配置缓存复用通过。详情见 [editable-delivery.json](evidence/editable-delivery.json)；实机接受仍待用户。低模按当前可编辑几何重新选面，远景观感待用户查看。

---

# 当前：原生 M4A1 展示方式统一

用户已接受上一版 e3504041；本轮保存点为实验室 69e6fc3c、NewMod ddefd4b。

本轮调整：
- 所有已接管实体组装的枪，Z 直接打开原有 WorkbenchScreen；删除原生 M4A1 中间跳转按钮。
- 复用雷电 convert_component 和 render_part_icon：标准局部坐标、+Z 枪口方向、完整 UV/原贴图及材质图标。相机和 SlotLayout 排布算法未改。
- 标签参考点选默认配件的几何中心，候选共用参考点；局部顶点与槽位一起重算，保证重组后中立几何不动。原生动画轴心保留在制作清单，不再直接用于工作台标签。槽位持久化 ID/安装树不变，补全中英文名称。
- 67 个部件/候选的独立 .bbmodel，加前瞄折叠版本；各件中不独立运动的几何合并，活动部分按动作归属分开。下机匣含 4 个网格，其余默认件各 1 个网格；不焊死枪机/拉机柄。
- 工作台和图标消费上述模型。持枪仍用经过验收的原生 rig/动画及高低模，不用静态编辑模型代替活动骨骼。

[逐件模型目录](../../../modules/tacz_adapter/weapon-sources/native_m4a1/README.md)（包含原 PNG、UV、来源哈希、运动归属、安装点、局部变换）。

验证：68 个模型在 Blockbench 临时独立项目中 load/compile，逐网格顶点及逐面顶点顺序/UV 完全一致。已恢复用户原项目，未保存覆盖。见 [Blockbench 证据](evidence/blockbench-components.json)。模型导出自身的中立重组误差小于 5e-11；工作台重新定位误差小于 1e-7。实验室 check/compilePureJava 已通过，最终制品关系随交付记录。

验收：原启动入口进入 NewMod，`/devitems` 找“原生实体组装”，Z 应直接进入我们的工作台。确认默认朝向与雷电一致，槽位不落到模型外的原动画轴心，图标有原始材质；然后检查拆装、两种护木、瞄具和保存重进。视觉与实机接受仍由用户进行。

---

以下为上一版交付历史，本轮说明优先：

# 原生 M4A1 实体组装交付

2026-09-16。第三项实施入口；授权见[完整实施合同](../整合与组装路线.md)。工程验证与制品关系记录在本目录证据文件；主控复核、用户实机接受分别记录，不能由构建通过推定。

## 新增内容

- 独立枪 ID：`tacz_assembly:m4a1`。原 `tacz:m4a1` 与 `newmod_adar:adar`、`newmod_radian:radian`、`newmod_m4a1:m4a1` 保留。
- 默认 15 个实体节点：下机匣根、上机匣、连接环、枪管、导气总成、前后瞄具、枪机、拉机柄、标准护木、手枪握把、缓冲管、CTR 枪托、30 发空弹匣、默认枪口；空膛。
- 共 15 个新注册物品身份（整枪根及 14 种零件，包含互斥的两种护木）；复用 52 种原生附件身份，未复制注册 `tacz:attachment`，未删除全局 85 附件。
- FMJ/HP/I 三种旧效果附件在本枪目录中没有安装位置。导轨护木才接受前握把/激光。M9 装在真实默认枪口下；换成其他枪口返还完整默认枪口/M9 子树。拆卸不免费再生默认件。

## 单一状态与运行链

装配保存在 foundation `AssemblyState` 的真实 ItemStack 树。安装先经过现有组装引擎，再由 tactical 完成扣除与返还；空间不足或旧报价失效时不写入。Z 与组装工作台共享 `AssemblyProposalSessions` 和 `AssemblyGunExchange`，会话单次使用、1200 tick 到期，原有持枪租约及库存版本复核保留。

原生附件查询读取树里的实体，倍率与激光颜色也写回该实体。不保存另一份 `GunAttachment` 数据。安装/卸载入口要求组装交易，不能通过原生存储接口偷偷创建副本。成功提交后刷新两侧属性；拒绝的提交不刷新新枪属性。

核心机构缺失禁射；无弹匣不能装填，移除弹匣退还现有弹药数、保留膛内一发。缺少两件机瞄且没有瞄具时不能进入瞄准。原 86 弹、口径固定退弹临时接口和既有精确弹种桥接不变，没有新增退弹键。

## 模型、动作与 LOD

制作入口：`tools/native_m4a1/generate.py`；原生文件只作输入。原 140 个骨骼及父子关系保留，几何放入独立叶节点按实体显示；不把安装树当动画骨骼树。17 个本枪动画、继承轨道、状态机、音效、装填时机、镜内渲染、倍率、挂点、枪口火焰与抛壳沿用原生资源。

高模资产共 938 个方块，低模 135 个方块；这是包含互斥变体的资产数量，不含独立附件，也不是每帧绘制量。68 个原生休眠 OEM 枪托方块不进入派生高模；CTR 使用实际附件渲染。低模保留并重绑 47 个原低模方块，原 28 个混合区域由分实体表面替换，补足两护木、四弹匣和折叠准星。每骨骼选取不超过两块较大原生表面作派生低模；细小结构简化，必须由所有者检查远景观感。

重绑中立矩阵最大误差约 `8.9e-16`。低模图集明确拼接原高/低模纹理；UV 使用对应区域。高低模调用同一实体遮罩，普通与加速渲染共享稳定叶节点，不改每帧 cube 列表，不建立装配组合缓存。原生低模加载器新增可选类型构造入口，只给新样本注册；旧枪继续使用原低模方式。

工作台使用原生中立几何与安装挂点的灰模预览；独立零件缩略图由几何生成。持枪仍使用原生贴图，不使用旧导入枪 meshScale 或二次姿态。各零件没有独立称重数据，本项没有虚构零件质量；枪械执行属性继续取原生枪数据和实际附件修饰。

## 所有者验收

1. 在 NewMod 使用既有 `启动开发客户端.command`。本任务不启动游戏、不修改日常世界。
2. 使用有权限的 `/devitems` 打开开发领取界面，按来源 `native_m4a1` 或名称 `M4A1 · 原生实体组装` 查找。默认枪与新零件由制作目录供给，原生附件也可在 `tacz` 来源领取。不要用普通 `/give` 裸 ItemStack 代替组装预设。
3. 持枪按 Z：原生附件改装；左上“打开组装工作台”进入完整实体拆装。两个界面应显示同一安装结果。标准与导轨护木是不同物品，不能装握把后自动变形。
4. 检查拆装、替换父件、装满背包时拒绝返还、连续提交、换手后旧操作；确认没有复制、丢件或免费补件。检查 M9 随默认枪口返还、拆下倍率/激光后再装仍保留状态。
5. 检查缺核心机构禁射、无弹匣不能装填但膛内一发可打、无瞄具不能瞄准；检查 30/40/50/60 发容量及真实弹种。
6. 检查持枪、机瞄、瞄具镜内与倍率、空仓/战术换弹、检视、挂机、射击、抛壳。切换两把不同装配的枪，检查近远、第三人称、地面、展示框及保存重进；检查低模细节是否可接受。
7. 回归原 `tacz:m4a1` 的 Z 改装、三把导入枪、弹药及防护链。

自动证据没有执行动画播放或测 FPS，也没有代替库存联机与实际渲染验收。第四/五项不在本次范围。

## 来源与回退

原模型/纹理/动作来自仓库原生 TaCZ M4A1，既有署名与许可证保留；派生资产仅纳入本地实验交付。逐路径前后哈希见 `../native-m4a1-integration.json`，历史迁移清单未重写。

回退基线：实验室 `2de081b3d16c6517a2db3da7ff2ce2886223fdfe`，NewMod `e45dd1f896adb4342781c88716c690d34a850372`，已接受制品 `1.1.8-hotfix-r6-newmod.556149d3`。恢复对应源码、制品和锁的完整组合；不要只换 Jar。旧制品保留，世界由所有者管理。

## 验收包接入记录

- 制品版本：`1.1.8-hotfix-r6-newmod.e3504041`。
- 实验室源码提交：`e3504041712124ccccf9daf8fc1e74c251c07ee6`。
- 主线接入提交：`aff72b2f9caaa9fd42f2fba4b43c73611014d5f2`，运行包、开发包及版本锁已同步。
- 实验室编译打包通过；按所有者最新要求不追加主线复查，不启动游戏。实机验收待所有者进行。
- 制品锁及交付状态见 [delivery.json](evidence/delivery.json)，构建记录见 [experiment-build.log](evidence/experiment-build.log)。

## 本轮最终制品

版本 `1.1.8-hotfix-r6-newmod.d59a0fcd`，源码 `d59a0fcd884dacc6553fdb8af70e24988d35f74c`，NewMod 接入 `fe848b41280273c40c61a5ae1bdeddf5cf87b172`。83 个 Java 测试通过；开发/正式运行唯一性、运行准备及配置缓存存储/复用通过，锁变化自动使缓存失效。本轮未启动游戏。详见 [本轮交付证据](evidence/workbench-delivery.json)。
