# TaCZ 实验室整合与组装路线

2026-09-15。用户已明确反馈前两项联合验收通过，并授权安排继续。当前进入第三项的原生 M4A1 拆件与动作关系审查，具体拆件清单依本路线在审查后交所有者确认。所有者负责实机验收，主控任务负责范围、证据复核与阶段推进，实施任务为 `01a0a4ee-875d-7113-832d-4d0a571ab340`（tacz 实验室）。

## 完整目标与当前事实

TaCZ 实验室以原生枪械完成真实配件、组装、库存、持久化、同步、外观、属性、开火限制及原生动作的完整链路。未来自有 firearms 接替枪械运行，组装规则、定位、界面和公共玩法接入可迁移。四个 weapon 与 adapter 已整合到单一 tacz 并获用户联合验收；原生 M4A1 仅有离线几何拆分，未成为游戏内组装枪。

最终一个 tacz Mod 承载四个 weapon 的内部模块和 adapter 专属职责。foundation、tactical、character、combat 继续独立；不迁入通用物品、库存、人物与伤害规则。不建立双引擎切换框架，不修改 firearms。

本文覆盖旧 README 中 A/B/C 的批次顺序及“weapon 保持独立 Mod”的建议；旧盘点保留作事实和迁移证据，不作为当前实施授权。

## 从完整目标倒推的结果链

| 项目 | 结果与 Todo | 前置 | Deferred 与归宿 | Constraint | 验收条件 | 待裁决项 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 weapon 合并 | 四个 weapon 成为 tacz 内部模块；迁移唯一源码、资源、测试和注册；NewMod adapter 消费新制品 | 当前已授权 | adapter 吸收归 2；原生拆枪归 3；旧枪退役归 5 | 规则与 TaCZ 执行隔离；身份不变；不复制运行类 | 四个独立 Mod 不再加载；新包与 adapter 联合构建通过，既有能力可供所有者实机验收 | 无产品阻塞项；内部包/普通子工程由实施者按边界选择 |
| 2 adapter 吸收 | TaCZ 专属桥接、三枪内容、网络与生命周期进入 tacz；逐个消除重复注入；NewMod 移除独立 adapter | 1 工程及主控复核通过；用户已授权合并验收，主控派发 2 | 新配件和拆枪归 3；旧枪退役归 5 | 单一状态所有者、事件与注册唯一；三枪兼容保留 | 只加载 tacz 即可承接原 adapter 能力；组装、Z 改装、供弹、命中、瞄准专项通过并获实机接受 | 不能等价内接的 Mixin 逐项留理由；行为变化交所有者裁决 |
| 3 原生 M4A1 完整组装 | 审查实体拆件边界，接入物品、装配、显示和原生动作，保留原枪对照 | 2 接受 | 其他枪与复杂覆盖归 4；旧三枪继续保留 | 原比例、UV、骨骼动画及互斥显示正确；手臂/相机不是配件 | 拆装保存与物品守恒；缺件限制；持枪、机瞄/瞄具、射击换弹经所有者接受 | 拆件粒度、具体配件清单在几何审计后由所有者确认 |
| 4 配件与组装完整覆盖 | 按实际机制补代表枪与配件，覆盖嵌套、冲突、属性、动画差异、供弹和多人一致性 | 3 接受 | 不无限扩展枪械目录；自有引擎接入归独立 firearms 路线 | 同一配件/弹药状态只有一个权威来源；保留既有效果 | 经裁决的场景矩阵全部有自动证据及必要实机接受；临时供退弹的替换条件处理完毕 | 代表枪清单、实体弹匣及精细动作范围，进入本项前裁决 |
| 5 旧三枪退役与成果交接 | 原生样本覆盖旧职责后，逐枪退出旧 TaCZ 入口，保留来源、制作资料、回归夹具和迁移说明 | 替代场景已接受；可逐枪开展 | 自有 firearms 模型/动作实现留其独立路线 | 不静默删除存档物品；不丢已验证规则和资产 | 旧入口不再用于主线验证；旧身份处理方案被接受；可迁移内部边界有证据 | 旧存档物品保留/转换/移除策略由所有者裁决 |

远期项目只定义结果，不宣称已授权实施或已完成。每项分别记录工程完成、主控复核、所有者接受，不以自动测试替代接受。

## 第一项实施合同

### 可修改范围

- 实验室仓库为主要实施位置。把 weapon_assembly、weapon_models、weapon_runtime、weapon_assembly_ui 收入 tacz；可保留普通 Java 子工程，但只发布/加载一个 tacz Mod，不保留四套 @Mod 与 Mod 元数据。
- 允许修改 NewMod 中必要的 settings/build、依赖元数据、adapter 消费入口、运行配置、资源/边界校验、vendor 制品及 dependency-lock，以形成真实可验收组合。
- 四个模块从 NewMod 迁出后不得留下另一份有效生产源码或重复类；旧路径消费者、测试夹具和脚本引用一起迁移。允许保留明确的历史资料，不能保留双份运行事实源。
- adapter 仍是独立 Mod；其组装、供弹、战斗等实现及三枪内容不在本项吸收。仅做消费新包所必需的调整。

### 必须保护

- 组装算法独立于 TaCZ 与 Minecraft，保留纯 JDK 编译验证；几何数学不依赖 TaCZ 具体渲染器；客户端 UI 不进入服务端类加载路径。
- 真实配件状态继续归 foundation，扣除返还继续调用 tactical；不建立第二套物品状态。
- 保留已有组件和资源身份，尤其 weapon_runtime:profile；撤销 Mod 入口不等于改资源命名空间。
- 不做枪械拆分、配件内容扩充、三枪退役或 firearms 修改；不改变供退弹语义。固定口径退弹临时接口及其缺口归后续，不因此增加退弹键。
- 15 枪、85 配件、86 弹的现有保留范围及抛壳等效果继续保护；许可与来源记录随迁移保留。

### 为什么允许暂留 adapter

第一项完成时运行组合为“tacz（含四个 weapon）+ adapter + 公共玩法 Mod”。adapter 暂留是为了让本次只验证模块合并且维持完整验收入口。第二项实施中删除其独立加载边界；不以当前过渡形态建设长期新接口。

### 工程验证与交付

1. 开始前记录两仓库分支、HEAD、工作区和制品基线；不覆盖其他任务改动，不创建新任务或 worktree。
2. 完成迁移清单：旧路径→新路径、注册/ID处理、生产与测试资源归属；新树保留原单测并执行受影响测试。
3. 检查 tacz Jar 包含四项能力但只声明一个 tacz Mod，NewMod 运行类路径没有四个旧 Mod 或重复类；检查正式包不夹带开发物品和测试夹具。
4. 执行实验室构建、相关规则与模型/UI测试、纯 Java 边界、资源保护检查，以及 NewMod adapter 正式/开发编译、资源校验、模块边界和运行配置准备。最终变更后检查最终树，记录失败与证据上限。
5. 可构建并更新本地版本化 Jar、SHA-512、源码提交及锁；不自动启动 Minecraft 客户端/服务端，不触碰或替换日常测试世界。保留可回退制品依据。
6. 提交两仓库各自的变更，明确源码提交、制品版本、NewMod 接入提交及两者对应关系；未获推送指令不推送。更新实验室改动记录。
7. 提供所有者可操作的验收清单：启动入口、领取现有三枪/配件的方法或准确命令、拆装/保存/持枪/瞄准/射击/换弹的预期、Z 原生改装回归、出现故障时回退办法。命令须查证，不能编造。
8. 第一项工程完成并经主控复核；按用户最新裁决继续第二项，第一项用户接受状态仍为待验收。第二项完成后一起验收，不进入第三项。

## 主控复核与路线重算

主控核对最终差异、依赖方向、制品唯一性、测试覆盖与用户验收入口；不直接替实施任务修改运行代码。若迁移必须改变物品身份、复制权威状态或进入原生枪拆分，先报告具体阻塞和最小调整，不自行跨项。前两项的问题在本次整合范围修复；用户拒收后先修复，不进入第三项。

## 记录与 RouteLedger

本次已通过 MCP 把运行绑定明确到实验室绝对路径；现场状态为 uninitialized，无 activeProject/current Version。未向 NewMod 历史路线写入，也未修改 canonical JSON。当前授权路线与验收门槛由本文承接。实验室独立项目初始化及中文内容语言确认后，通过 RouteLedger MCP 建立对应路线并链接本文；在此之前不得声称 RouteLedger Version 已创建或关闭。Mission Control 当前不可用，不阻塞实施。

## 第二项实施合同（用户授权继续，前两项联合验收）

- 目标组合：tacz（四个 weapon 内部能力、原 adapter 专属接入、现有三枪内容）+ 独立公共玩法 Mod；不再安装或加载 tactical_tacz_adapter。保留原有三枪，不做退役或原生枪拆分，不改 firearms。
- 逐文件迁移 adapter 的正式/开发代码、资源、weapon-content、制作输入/工具/报告、测试及必要文档。明确每类资料归属；删除旧生产入口后不得留下双份事实源。开发内容不进入正式 Jar。
- 把原 adapter 的初始化、网络、组装交易、Z 改装、供弹 HUD、弹道命中、人物瞄准限制接入 tacz；服务端权威、会话过期、防重放、物品原子交换和事件顺序保持。逐个核对 Mixin，在 TaCZ 自有调用点等价吸收并退役旧注入；无法内接者逐项说明边界与理由，不以两套逻辑同时运行交付。
- 保留 newmod_adar/newmod_radian/newmod_m4a1、weapon_runtime:profile 及 adapter 既有组件/协议/资源身份；不因取消 Mod 入口而强制改 ID。真实装配仍归 foundation，库存归 tactical，人物归 character，伤害防护归 combat。
- 更新 NewMod 的构建、分发、运行组合、开发命令、日常启动入口及版本锁，使主线可直接验收新包。区分生产依赖和开发内容，核查 tarkov_content 实际消费者后决定依赖，不为图形简洁强行删依赖。公共制品来源与哈希可追溯，不嵌入公共 Mod 或重复类。
- 当前 adapter 的精确弹种退还行为必须保留；默认固定口径临时接口继续按原适用范围运行，不新增退弹键、不扩展实体弹匣。保留原生枪、配件、弹药和效果保护检查。
- 最终执行受影响单测、纯算法边界、资源/内容/注册/唯一性检查、实验室构建与 NewMod 正式/开发编译、分发和运行准备。行为性回归覆盖组装与 Z 交易、供弹返还、开火门槛、命中与瞄准；未能自动验证的场景明确列入用户清单。不得只以编译通过替代行为证据。
- 不自动启动客户端或服务端，不改日常世界；用户亲自联合验收。交付原三枪、原生 Z 改装、供弹/换弹、瞄准开火、伤害防护、保存重进等可操作步骤，区分原有问题与新增退化。
- 两仓库分别提交，不推送；交付迁移清单、源码/制品/哈希/NewMod 接入提交关系及整体回退办法。保留第一项基线，以便第二项回退；不可只替换 Jar。
- 第二项工程完成后交主控复核，状态为“前两项待用户联合验收”。第三项仍未授权。

## 用户接受与下一步（最新状态）

用户明确反馈“我验收通过了，可以安排继续了”。第一、二项记录为用户联合接受；依据为本次用户反馈，不推断每个子场景的单独测量结果。已接受制品源码为 556149d3，NewMod 接入含缓存修复 e45dd1f。前文第一、二项实施合同中的待验收/不进入第三项属于当时阶段边界，由本节最新授权覆盖。

第三项先交付可裁决的拆件与动作清单：基于原生 M4A1 核对当前模型与离线样本哈希，逐骨骼/几何确定实体件、安装关系、动画所有权、互斥显示、瞄具挂点和弹匣状态。重点审查 receiver_remainder，不把18个几何分区直接当18个配件；手臂、相机和表现弹药不可注册成可拆件。

审查产出包含建议拆件表、默认装配、与85个既有原生附件的关系、原生Z改装与实体组装如何避免双重状态、原生动画保真接入方案、可验证范围及待所有者裁决项。先读现有代码证据；可更新离线审查脚本/报告，不修改生产枪或制品，不启动游戏，不改当前Blockbench项目。主控复核后向用户说明具体清单，再据确认派发完整游戏接入。第三项不扩展其他枪，不退役旧三枪。
