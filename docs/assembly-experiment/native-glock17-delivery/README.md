# 第二把原生组装枪：Glock 17

2026-09-16。所有者授权继续扩大 TaCZ 原生组装覆盖；本批选择结构不同的 Glock 17，验证公共制作与运行机制。制作、运行接线、完整构建与 NewMod 制品接入均通过，未实机验收。

## 本批合同

- 结果：独立可编辑的枪体配件和贴图，接入实体组装、工作台、完整持枪/物品图标、原生动作及公共附件；NewMod 更新后由所有者启动验收。
- Todo：审查每个原生 cube 的实体归属；生产可重复构建的编辑源与运行资源；去除显示/装备注册中的 M4 假设；验证拆装、弹匣、瞄准、动画引用和 M4 回归；提交并接入。
- Constraint：原版枪包、M4 已接受资产、日常世界保留；物品树是唯一装配状态；不新增枪名分支、不改变供弹权威；不启动游戏、不推送。
- Deferred：其他 13 把原生枪归后续批量接入；瞄具制作最后进行；工作台与持枪的照门/准星姿态共享在配件扩面之后推进。本批只复用原光学候选及原生显示。
- 验收：离线几何/资源/自动测试和双制品集成检查由 agent 完成；射击、换弹、瞄准、第三人称、库存图标与手枪栏由所有者实机接受。
- 待裁决：目前无；如原模型不支持合理实体拆分，先记录证据并上报，不臆造零件。

## 完整覆盖顺序

M4 和公共非光学附件已完成前序接入 → 本批 Glock 不同结构验证 → 剩余枪械按配置批量接入 → 共享机械瞄具显示姿态 → 光学瞄具制作。

当前不将未核实绑定的 RouteLedger 历史节点作为状态权威；本批记录和证据保存在本入口，后续随项目绑定核对再迁入正式路线。

## 已核实的模型边界

原生高模 57 骨、264 cubes：默认六件 196 cubes、三扩容变体 48 cubes、手部/弹药展示 20 cubes，全部有审计归属。光学安装沿用 Glock 原生机械瞄具不折叠的规则。

原低模仅 12 骨、20 cubes，缺少扩容变体与光学/枪口挂点，不能按名称机械拆分。本批远距离路径暂使用完整可编辑几何并保留同一原生骨架，确保配件身份和动画一致；这不是完成减面优化，也不声明 FPS。后续批量扩大枪械前，用经过外观验收的逐配件简化几何替换该低模资源；运行机制和实体 ID 不变，不另建临时渲染分支。

## 工程结果与验收入口

默认六件加 14 个原生兼容候选（3 扩容、3 消音器、2 激光、6 只读光学），共 20 项目录。枪口/激光直接复用已交付公共资源；新工具和每枪配置负责工作台、图标及原生渲染投影。光学外观投影仅含原生非第一人称的外壳/镜圈，不把特殊分划平面计入取景，不修改第一人称光学逻辑。

最终 `check assemble --offline` 通过：125 项 Java、6 项本枪生产专项、12 项 M4 编辑源、9 项公共附件、14 项原有制作链 Python 检查通过。六新增件真实 Blockbench 往返误差 0；独立对原数据/骨元信息/逐面 UV/图集像素核对通过；重复生成不改哈希。原枪包、既有 M4 编辑源及公共附件编辑源没有改动。

[完整默认装配 CPU 图标](evidence/assembled-icon.png)用于离线外观核对，不是游戏截图。

NewMod 接入完成后，从 `/devitems` 领取 **Glock 17 · 原生实体组装**（来源 `native_glock_17`，ID `tacz_assembly:glock_17`），不要拿原版对照枪。建议依次检查：

1. 手枪装备栏、第一/第三人称、快捷栏/装备图标和掉落展示完整，枪口朝左。
2. 拆套筒/枪管后缺件限制及显示，装回恢复；两个机械瞄具齐全时机瞄可用。
3. 默认 17 发与 20/25/30 发扩容换弹、空仓挂机；拆匣显示与膛内弹状态。
4. 三消音器、两激光挂点和效果；既有光学候选仍走原生逻辑。
5. 切回已接受 M4 检查原显示和机械瞄具卧倒规则。

用户负责实机接受，agent 未启动客户端、服务端或修改世界。

制品 `1.1.8-hotfix-r6-newmod.30acf818`；源码 `30acf818`，NewMod接入 `d4634bf`。正式927/开发968个集成类，唯一制品、哈希、启动准备及配置缓存存储/复用通过；默认开发启动准备已恢复。详见[evidence/delivery.json](evidence/delivery.json)。未推送远端。
