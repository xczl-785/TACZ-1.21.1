# TaCZ 全仓文档治理

2026-09-19。用途：让接手者找到当前生产与维护入口，区分已结束批次、现行机制、仍被程序读取的台账和未关闭边界。检查基线为 `d63baddb`；光学源码 `a3bc3d4e` 已获所有者整体接受。本次使用 docs-governance 与 document-purpose，不更改代码、生成资源、运行锁或既有 JSON 门禁输入。

## 当前阅读顺序

[工程入口](README.md) → [当前任务](任务盘点.md) → [状态](../../status.md) → 所需[模块](../../modules/README.md)、[作者源](../../modules/tacz_adapter/weapon-sources/README.md)或[交付证据](../assembly-experiment/README.md)。

用户裁决约束范围，当前代码/作者输入/构建配置说明实际实现，运行锁说明实际加载版本。日期较新的报告不会自动推翻源码或补齐实机证据。原版附件仍与新件并存；统一 `tacz_fork_tarkov` 和退役原件是已确认的下一阶段方向，尚未执行。

## 盘点边界与逐文件清单

[document-inventory.csv](governance/document-inventory.csv)逐项列出路径、主角色、过程状态、事实来源、消费者、退出条件与退出后归宿。[link-findings.csv](governance/link-findings.csv)保留历史断链，不把失效链接改造成假作者源。

覆盖本仓库全部受版本控制 Markdown/说明文本；`docs/**`、`modules/**/docs/**`、旧制作报告树的全部文件；作者源下命名为 report/audit/evidence/verification/check 的 JSON；公共依赖锁与夹具来源台账。既有文本文档已读取并扫描标题、状态、路径与消费者；JSON已解析。图像、HTML、日志按证据归属登记，不重新执行其历史测试或重作画面验收。

不把数千个模型/运行 JSON 逐一当文档改写：作者配置、生成资源、输入素材、测试夹具由下表明确边界和对应工具负责。本次不声称完成所有代码行为或每份旧报告事实的重新验证；未解析动态拼接引用一律不能据此判定资源可删除。代码消费者栏为仓内源码对完整路径的直接引用扫描，可能还有间接消费者；整套 JSON 台账均保持原位。

| 子树 | 当前角色与路由 | 保护 / 后续条件 |
| --- | --- | --- |
| 根 readme/AGENTS/status | 上游说明与许可、接手规则、当前状态；状态由本次已接受事实更新 | 原许可证及上游说明保留；历史 RouteLedger 标识不当当前绑定 |
| docs/newmod | 当前导航、任务、追加日志；cleanup/inventory/extra-content 是早期选择和清理证据 | 选择与删除 JSON 仍被校验器消费，不能归档移走 |
| docs/content-isolation | 首次 54/45/9 隔离快照与资源哈希 | 不是当前枪/附件数量；live selection/inventory 继续原位保护 |
| docs/assembly-experiment 根 | 当前导航；canonical cutover/弹药边界继续适用；融合前诊断与旧路线保留追溯 | 移出的旧 README 正文完整保留在[融合前职责盘点](../assembly-experiment/融合前职责盘点.md)，相对链接目录不变 |
| native-m4a1-review | 初次拆件审查快照 | audit/ownership-proposal 仍是 export_parts 的初次提取输入，不是日常重生成入口，不移走 |
| native-m4a1-delivery / native-glock17-delivery / native-batch-one / native-attachments-delivery | 分批交付、独立审核、日志、截图 | 旧“下一枪/瞄具延后/未激活”按批次理解；现有作者配置和全量光学入口接替执行 |
| native-remaining | 十枪与工作台后端/场景修复证据，保留待核对状态 | 未找到最终工作台专项明确接受，不由瞄具验收补签；RenderTarget 条件保留 |
| optics-samples / optics-batch | 两件样本已接受；全量 28 件已整体接受并作为维护入口 | package-proof/report 为对应提交证据；三件 LOD 回退仍保留；替换原件另立逐 ID 闭环 |
| modules/* 顶层与 weapon_assembly/docs | 内部模块入口、算法合同、早期原型/测试设计 | 旧独立 Mod 和命令已局部标记并统一转到 modules/README |
| modules/weapon_assembly_ui/docs | 组装台当前行为合同与待验证矩阵 | 产品矩阵、V2 触发条件不随光学接受自动关闭 |
| modules/tacz_adapter/docs | 接入机制与历史三枪制作证据 | 旧三枪流程/表现/材质标为退役生产链；来源与历史证据不删除 |
| modules/tacz_adapter/weapon-sources | 十五枪、非瞄具、光学的唯一作者源；现行 README 指导编辑 | 原生骨架/UV/功能面合同保留；配置和 manifest 属生产输入，不做文档整理式改写 |
| modules/tacz_adapter/weapon-content | 生成输出说明 | 内容 JSON/模型/贴图只经生成器维护，不手动改身份 |
| modules/tacz_adapter/weapon-reports | 已退出生产的三枪历史报告 | 保留哈希与来源，不恢复作者源，不当当前生成输入 |
| modules/weapon_models 测试夹具 / weapon_assembly/model-fixtures | 数学回归数据和测试说明 | fixture-sources 及检查器合同保持；完整旧网格已经 V3 收敛，不恢复 |
| tools/native_* | 现行工具说明 | M4 专用生成器、其余枪公共生成器、光学 batch 各自明确，不恢复已删除的旧 weapon_pipeline |
| ammunition | 86 弹制作说明、批准输入、生成与迁移对账 | 独立于光学身份替换；不改实际弹药资源、身份或临时退弹行为 |
| src 内 README.txt / 许可证 / KubeJS txt | 上游包说明、许可、运行注册/过滤文本 | runtime 文本不是闲置文档；不改、不移动、不删 |

## 治理闭环台账

每一主题只选择一个动作桶；结果状态不等于功能或代码问题已解决。

| 主题 | 动作桶 | 结果状态 | 已处理 / 保留条件 |
| --- | --- | --- | --- |
| 高层入口仍指旧运行锁、M4 初轮、融合前建议 | auto-fix | fixed-now | 改造入口、实验索引、任务与状态从当前已交付事实组织；旧路线仅批次追溯 |
| 模块已合并但 README 仍声称独立，adapter 仍说三枪保留 | auto-fix | fixed-now | 更新单一包/公共 API 依赖；局部旧模块文档明确命令由 modules 入口接替 |
| 旧三枪制作/材质/简单红点说明被当执行入口 | safe-isolation | safe-isolated | 原文保留并标退役生产链，工具入口改到仍存在的 native 工具；不批量修复已删除素材链接 |
| 光学作者源仍写未验收、状态仍停两件样本 | auto-fix | fixed-now | 仅按 d63baddb 和本轮用户反馈回写 28 件整体接受；不虚构逐项测试 |
| 公共非瞄具未激活、Glock 未减面、UMP 无光学源等过期状态 | auto-fix | fixed-now | 由后继交付与现行显式配置替代，原报告计数仍按其日期理解 |
| 活跃机制文档跨仓链接失效 | auto-fix | fixed-now | 修正存在且唯一的 NewMod 目标；独立克隆不承诺存在同级仓库 |
| 旧证据中路径失效或旧数量与现状不同 | safe-isolation | safe-isolated | 逐链接记录；只沿当前入口操作，保留来源原文及旧哈希 |
| 文档目录内有 live JSON 与代码后继门禁 | safe-isolation | safe-isolated | 逐路径列出直接消费者，保持全部既有 JSON 和 gate 输入字节不变 |
| check 历史迁移台账不匹配 | safe-isolation | deferred-with-condition | 不改哈希、基线或豁免；代码维护者补完整后继范围并重跑对应及全量 gate 后关闭 |
| 十枪末次工作台修复缺最终实机接受回写 | safe-isolation | deferred-with-condition | 维护者定位该修复对应的明确所有者反馈后更新；本轮光学接受不代签 |
| 三件光学和其他资产的全几何 LOD 回退 | safe-isolation | deferred-with-condition | 另批具备减面作者源和几何/视觉比较再替换；本次不宣称性能改善 |
| V4 依赖裁剪 / RenderTarget 条件 / 组装台待验矩阵 | safe-isolation | deferred-with-condition | 分别按状态与原合同触发条件重开，不由“TaCZ 大任务基本完成”统一删掉 |
| 原版附件替换的旧物品与保存数据处理 | human-decision | human-decision-queued | 方向已定，不重复询问是否统一前缀；执行退役前由内容盘点给出旧→新 ID、兼容/数量/状态保全及无法映射条目，建议优先可追溯迁移，再由所有者确认具体存量处置 |
| 早期描述重量、实体弹匣、精确退弹替换 | safe-isolation | deferred-with-condition | 旧合同只保留依据；对应行为任务启动时先核对当前实现和确定范围，不因本文新造实现任务 |

## 验证与未改变

- 对当前入口与新链接做路径存在性检查；历史失效链接全部留在 link-findings，不声称全仓零断链。
- 逐文件清单覆盖上述范围；既有 JSON/日志/图片和 source/runtime/build 输入未修改，历史融合 README 正文完整保留。
- `git diff --check` 作为本次文档变更检查。未运行游戏、未覆盖世界、未更新 Jar/锁、未推送，也未重跑全部工程测试。
- 全量 `check` 已知门禁问题仅记录，不在文档治理中改变检查语义。下一阶段物品替换、命名空间和依赖图由主任务盘点，本页不提前执行退役。
